//trying to fix darkmode. it copies my os preference so the toggle doesn't work.

export default {
  plugins: {
    "@tailwindcss/postcss": {},
    autoprefixer: {}, // if you need autoprefixer
  },
};
